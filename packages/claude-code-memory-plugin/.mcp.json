{
  "mcpServers": {
    "memory": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/server.js"],
      "env": {
        "MEMORY_CLAUDE_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}",
        "MEMORY_CLAUDE_PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
